import { Meta, Story, Props } from '@storybook/addon-docs/blocks'
import TestComponent from './index.tsx'

<!-- Here we define metadata for the story, the way its categorized, define the props, etc -->

<Meta
  title='Test/Docs'
  component={TestComponent}
  argTypes={{
    name: { control: 'text', description: 'testing', type: { required: true } },
  }}
  parameters={{
    previewTabs: {
      'storybook/docs/panel': { title: 'Main', index: -1 },
    },
    backgrounds: [{ name: 'dark', value: '#333' }],
  }}
/>

# New Storybook Format Testing

This is an mdx story using the latest storybook standards. Cool stuff!

<!-- Here's how we embed a story - all the exported stories show up standlone in the "Canvas" tab for free -->

<Story name='Basic' args={{ name: 'jeff' }}>
  {((args) => <TestComponent {...args} />).bind()}
</Story>

<!-- This is how we display the controls/props table -->

<Props story='Basic' />

now we can put more markdown an explanation, then embed more stories if necessary!
